schema_version = "1.3"
artifacts = []

[task]
name = "Amol07/reconcile-batch-task"
description = "Fix a nightly settlement reconciliation job so it reads its tolerance and per-currency minor units from config, quarantines malformed rows instead of aborting the batch, and writes the specified report and quarantine files."
authors = [{ name = "Amol Prakash", email = "amolprakash06@gmail.com" }]
keywords = ["processing", "python", "csv", "decimal", "data-validation", "finance"]

[metadata]

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]

[agent]
timeout_sec = 1800.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
